// Define the multiply variable with an equation whose result is 62
const num1 = 31;
const num2 = 2;
const multiply = num1 * num2;

// Define the random variable to generate a random integer greater than 0
const random = Math.floor(Math.random() * 100) + 1;

// Define the mod variable with an equation that calculates a remainder of 4
const num3 = 16;
const num4 = 6;
const mod = num3 % num4;

// Define the max variable to return 20 as the highest number in a set
const max = Math.max(1, 2, 3, 20);
